package com.yunnext.pad.app.repo.uart

import android.util.Log

private const val TAG = "_ble"

fun d(msg: String) {
    Log.d(TAG, msg)
}

fun e(msg: String) {
    Log.e(TAG, msg)
}

fun w(msg: String) {
    Log.w(TAG, msg)
}

fun i(msg: String) {
    Log.i(TAG, msg)
}